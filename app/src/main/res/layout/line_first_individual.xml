<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <ImageView
        android:layout_marginTop="0dp"
        android:contentDescription="@string/label"
        android:id="@+id/imgBGIndividual"
        app:layout_constraintTop_toTopOf="parent"
        android:scaleType="center"
        android:src="@drawable/imgpdf"
        android:layout_width="match_parent"
        android:layout_height="200dp" />

    <ImageView
        android:id="@+id/imgAVTIndividual"
        android:contentDescription="@string/label"
        android:background="@drawable/circle"
        android:layout_marginTop="125dp"
        app:layout_constraintTop_toTopOf="@id/imgBGIndividual"
        android:layout_width="150dp"
        android:layout_height="150dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent" />
    <TextView
        android:layout_marginBottom="10dp"
        android:id="@+id/tvNameIndividual"
        android:layout_marginTop="10dp"
        android:textColor="@color/black"
        android:textSize="20sp"
        android:text="@string/label"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@id/imgAVTIndividual"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</android.support.constraint.ConstraintLayout>